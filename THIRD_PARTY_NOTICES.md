# Third-party notices

The current source uses `modernc.org/sqlite v1.47.0` and the transitive Go
modules pinned in [go.mod](go.mod) and [go.sum](go.sum). It is not dependency-free.

The complete distribution notice bundle is pending. Before any public binary
release, inventory the dependencies actually included in the build, verify each
license and copyright notice against the dependency source, include required
license texts, and generate the release [SBOM](SBOM.md).

This file is an explicit incomplete-materials marker, not a substitute for the
third-party notices required by a binary distribution. Do not mark the release
ready until the verified inventory and license materials replace this marker.
